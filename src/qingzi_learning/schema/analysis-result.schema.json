{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Qingzi homework analysis result",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "document_id",
    "subject",
    "subject_confidence",
    "document_type",
    "source_exam_id",
    "grading_mode",
    "teacher_mark_evidence",
    "page_subjects",
    "questions",
    "summary"
  ],
  "properties": {
    "document_id": {"type": "string", "minLength": 1},
    "subject": {"enum": ["语文", "数学", "英语"]},
    "subject_confidence": {"type": "number", "minimum": 0, "maximum": 1},
    "document_type": {"type": "string", "minLength": 1},
    "source_exam_id": {"type": ["string", "null"], "pattern": "^QZ-[A-Z0-9-]+$"},
    "grading_mode": {"enum": ["teacher_marked", "auto_grade", "mixed"]},
    "teacher_mark_evidence": {
      "type": "array",
      "items": {"type": "string"}
    },
    "page_subjects": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/page_subject"}
    },
    "questions": {
      "type": "array",
      "items": {"$ref": "#/$defs/question"}
    },
    "summary": {"type": "string"}
  },
  "allOf": [
    {
      "if": {
        "properties": {"grading_mode": {"const": "teacher_marked"}},
        "required": ["grading_mode"]
      },
      "then": {
        "properties": {
          "questions": {
            "items": {
              "allOf": [
                {"$ref": "#/$defs/question"},
                {
                  "properties": {
                    "decision_source": {"enum": ["teacher", "mixed"]}
                  }
                }
              ]
            }
          }
        }
      }
    }
  ],
  "$defs": {
    "page_subject": {
      "type": "object",
      "additionalProperties": false,
      "required": ["page", "suggested_subject", "confidence", "needs_confirmation", "reason"],
      "properties": {
        "page": {"type": "integer", "minimum": 1},
        "suggested_subject": {"enum": ["语文", "数学", "英语"]},
        "confidence": {"type": "number", "minimum": 0, "maximum": 1},
        "needs_confirmation": {
          "type": "boolean",
          "description": "单页含多个学科或页面内容不足时，无论置信度高低，都必须设置 needs_confirmation=true；学科判断置信度较低时也必须设置为 true。"
        },
        "reason": {"type": "string", "minLength": 1}
      }
    },
    "question": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "question_id",
        "source_exam_question_id",
        "question_type",
        "page",
        "prompt_summary",
        "student_answer",
        "reference_answer",
        "status",
        "decision_source",
        "knowledge_points",
        "error_categories",
        "confidence",
        "reason"
      ],
      "properties": {
        "question_id": {"type": "string", "minLength": 1},
        "source_exam_question_id": {"type": ["string", "null"], "pattern": "^Q[0-9]{2}$"},
        "question_type": {"enum": ["objective", "fill_blank", "calculation", "application", "reading_open", "composition", "translation", "short_answer", "other"]},
        "page": {"type": "integer", "minimum": 1},
        "prompt_summary": {"type": "string"},
        "student_answer": {"type": "string"},
        "reference_answer": {"type": "string"},
        "status": {"enum": ["correct", "incorrect", "partial", "needs_review"]},
        "decision_source": {"enum": ["teacher", "model", "mixed"]},
        "knowledge_points": {
          "type": "array",
          "items": {"type": "string"}
        },
        "error_categories": {
          "type": "array",
          "items": {"type": "string"}
        },
        "confidence": {"type": "number", "minimum": 0, "maximum": 1},
        "reason": {"type": "string"}
      }
    }
  }
}
