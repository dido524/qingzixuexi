# 针对性模拟试卷验收记录

日期：2026-09-18（Asia/Shanghai）。验收使用正式资料库的只读副本和临时知识库目录；没有修改孩子正式资料库，也没有发送物理打印任务。

## 功能结论

- 家长可选择学科、范围、时长、难度、题量以及阅读/作文选项，必须先预览本地计算的组卷蓝图，再调用模型生成。
- 题目由独立的生成、校验和本地验证三道门共同约束；仅允许一次修复，仍不合格则失败关闭，不能发布。
- 家长批准前只能预览；批准后分别得到学生试卷、答题纸、答案与解析、组卷说明和清单，四个打印入口互相独立。
- 学生试卷和答题纸不包含答案或隐藏解答数据。打印出来的试卷编号和题号可在后续回拍时连接成复测证据。

## 正式数据副本闭环

验收根目录：`C:\Users\Home\AppData\Local\Temp\qingzi-targeted-exam-acceptance-srlno8u1`。

- 从正式数据副本识别到数学薄弱点“小数加减法”。
- 试卷编号：`QZ-MATH-20260918-003823-EC765E`。
- 草稿状态 `needs_parent_approval`、修订号 1；批准后状态 `approved`。
- 共 5 题、总分 100 分；学生材料答案泄漏检查结果为空。
- `exam.json` 中学生试卷、答题纸、答案解析和组卷说明的文件哈希均与实际文件一致。

## 复测连接与防伪

- 有效回拍首次写入 1 条 `exam_attempts`；重复提交同一资料后仍为 1 条，没有重复计入。
- 伪造试卷编号和跨学科编号均进入 `needs_review`，不产生复测记录，也不影响掌握统计。
- 后续报告包含 1 条复测记录；孩子版只说“这次复测已答对，建议再确认一次”，没有把一次答对夸大为稳定掌握；家长版保留完整追踪信息。

## 打印预览

四个批准后的 HTML 均包含 A4 规则、试卷编号、稳定题号和显式 `window.print()` 入口。

- 学生试卷：2 页，A4 纵向。
- 答题纸：2 页，A4 纵向。
- 答案与解析：最终渲染为 5 页，每题独立分页并保留 12 mm 页首间距，消除了上一版 Q04 跨页截断。
- 组卷说明：1 页，A4 纵向。
- 所有页面已逐页查看，没有文字裁切、重叠、黑块或答案混入学生材料；没有执行物理打印。

## 自动化与分发

- 重基到 `origin/main` 后执行完整测试：`593 passed, 1 skipped in 416.97s`；唯一跳过项是本机缺少 Windows symlink 创建权限。
- 分发包构建成功，包内分析、报告、试卷生成、试卷校验 Schema 和数据库 Schema 完整；分发包与安装后 EXE 的 SHA-256 均为 `7F878D9DD0D2AE2E62A77A555E34242DDC6F4397A9AA656DCEF2901F500E0F2F`。
- 安装后 `--smoke-check` 退出码 0，应用残留进程数 0；桌面快捷方式继续使用照片图标。
- 安装前已备份正式 SQLite；安装和首页刷新只添加新功能所需表结构和入口，没有写入验收试卷、验收成绩或合成资料。

