# 数学双视图知识图谱交付验收

验收日期：2026-09-22（Asia/Shanghai）

## 自动化回归

- 最终聚焦回归：`84 passed in 57.80s`，命令覆盖图谱、存储证据资格、首页、发布锁和打包契约。
- 最终完整测试：`756 passed, 1 skipped in 2056.31s (0:34:16)`。
- 图谱新增测试没有跳过；唯一跳过项是项目原有的条件性用例。
- 打包契约：`11 passed in 2.76s`。
- PyInstaller：6.22.3；打包程序版本 `0.1.0`；程序自检退出码 `0`。
- 安装包与正式安装 EXE 的 SHA-256 一致：`645762C388165BD383CDC7FAF8BBAC50628C93D066D56CB387629CA64F106FF2`；安装后自检退出码 `0`。
- 程序自检会在系统临时目录内加载图谱、课程映射并实际生成两张页面，确认两者嵌入数据相同；不打开摄像头、不调用模型、不写入用户知识库。

## 真实数据副本

验证使用 SQLite 只读连接和 `backup()` API 创建的一致性副本，输出到系统临时目录；没有复制题目正文到验收记录。

- 规范节点：62
- 横向知识联系：26
- 其中高重要度联系：17
- 已确认标签：8
- 未映射标签：209
- 跨学科标签：7
- 有掌握证据的节点：6
- 本地断链：0
- 外部资源：0
- 两张页面的嵌入数据：完全一致
- `情态动词can`、`like doing`、`would like to do`、`一般将来时`：均为 `cross_subject`，未进入数学节点统计
- 副本操作前后源数据库哈希：一致

## 安装与数据保护

- 安装位置：`C:\Users\Home\Documents\QingziLearningAssistant\app`
- 桌面快捷方式目标：`C:\Users\Home\Documents\QingziLearningAssistant\app\晴子学习助手.exe`
- 安装前备份：`C:\Users\Home\Documents\QingziLearningAssistant\backups\pre-math-graph-20260922-023030`
- 最终复核修正安装前备份：`C:\Users\Home\Documents\QingziLearningAssistant\backups\pre-graph-review-fix-20260922-035824`
- 备份内容：旧程序、旧数据库、快捷方式、备份清单，以及启动恢复后的 SQLite 一致性备份。
- 最终复核修正备份另含模型设置和 DPAPI 加密的 DeepSeek 密钥；源数据库和备份数据库 `integrity_check` 均为 `ok`，均包含 16 张业务表。
- 恢复后备份：`knowledge-restored-current.sqlite3`，大小 1,159,168 字节；与当前数据库各表行数一致。
- 当前数据库与安装前只读一致性副本：16 张表的集合、行数和逐表内容哈希全部一致。

## 实际知识库输出

- `C:\晴子知识库\5th grade\知识库首页.html`
- `C:\晴子知识库\5th grade\数学\课程体系\primary-math-v1\数学知识全景脑图.html`
- `C:\晴子知识库\5th grade\数学\课程体系\primary-math-v1\数学掌握知识图谱.html`

实际输出再次通过离线校验：62 个节点、26 条联系、0 断链、0 外部资源。两张页面已通过 Codex 内置预览入口打开。

## 页面与交互契约

自动化测试覆盖并确认：

- 工具栏可换行；宽屏为图谱加侧栏，窄屏切换为单列并缩短画布高度。
- 图例、详情面板、键盘焦点、ARIA 状态播报存在。
- 全景脑图包含四大领域、层级关系和非层级横向关系，不是单纯目录树。
- 搜索、全部小学、当前年级、本学期、重要/全部联系、恢复全貌、全屏、打印均存在。
- 掌握图谱包含学段、年级、学期、教材与课程、掌握状态、核心素养和短板模式筛选。
- 学期和观看范围筛选会真实隐藏 SVG 节点与关联线；待确认教材来源仅进入审核区，不会显示为孩子已学内容。
- 点击节点可显示前置路径、后续应用、已确认教材/课程来源、累计走势和可追溯的确认题目；未确认题目和低置信度模型题不会进入该列表。
- 少于 5 个确认样本时累计走势显示样本不足；`steady` 已中文化为“稳定”。
- 打印样式和薄弱知识点附录存在，打印清单随当前筛选同步；页面不使用 `localStorage` 或远程 CDN，离线验证器也拒绝协议相对网络地址。

浏览器自动化不能绑定 `file://` 本地页面，安全策略拒绝了直接页面截图和脚本化点击；没有尝试绕过。该限制不影响页面生成、离线校验、打包、安装或用户在本机预览中的使用。
